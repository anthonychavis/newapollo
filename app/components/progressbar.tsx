type Props = {
    children: string;
};

// temp fxn component body
export const ProgressBar = ({ children }: Props) => (
    // <figure>
    //     <figcaption>{children}</figcaption>
    // </figure>
    <p>{children}</p>
);
